{
  "name": "repohosting",
  "version": "0.1.0",
  "private": true,
  "description": "RepoHosting - a simple UI for speedy deployments to the platform of your choice (Vercel, Render, Netlify).",
  "type": "module",
  "scripts": {
    "serve": "http-server frontend -p 4173 -c-1 --silent",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "keywords": [
    "vercel",
    "render",
    "netlify",
    "supabase",
    "deployments"
  ],
  "author": "@sangal1",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "http-server": "^14.1.1"
  }
}
